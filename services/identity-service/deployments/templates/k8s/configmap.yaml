apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "identity-service.fullname" . }}-config
  labels:
    {{- include "identity-service.labels" . | nindent 4 }}
data:
{{- with .Values.config }}
{{- toYaml . | nindent 2 }}
{{- end }}
