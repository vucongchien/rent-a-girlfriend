syntax = "proto3";

package identity.v1;

option go_package = "github.com/rent-a-girlfriend/identity-service/gen/proto;identityv1";

// RefreshTokenRequest — đổi refresh token lấy access token mới.
message RefreshTokenRequest {
  string refresh_token = 1;
}
