syntax = "proto3";

package identity.v1;

option go_package = "github.com/rent-a-girlfriend/identity-service/gen/proto;identityv1";

// LogoutRequest — thu hồi refresh token, kết thúc phiên đăng nhập.
message LogoutRequest {
  string refresh_token = 1;
}
